{
  "name": "shirtmanufakturhol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-index.mjs && vite",
    "build": "node scripts/prepare-index.mjs && vite build && node scripts/publish-static.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
